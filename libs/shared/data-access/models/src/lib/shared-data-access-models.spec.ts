import { sharedDataAccessModels } from './shared-data-access-models';

describe('sharedDataAccessModels', () => {
  it('should work', () => {
    expect(sharedDataAccessModels()).toEqual('shared-data-access-models');
  });
});
